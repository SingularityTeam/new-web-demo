/**
 * 该文件是为 AR 定制的 antd 主题配置
 */

'use strict';

const customTheme = {
  '@primary-color': '#23b7e5', // 全局主色

  '@layout-header-background': '#19233c', // `dark`主题背景色

  '@menu-collapsed-width': '48px', // 菜单折叠时的宽度
  '@menu-dark-color': '#bad0e8', // `dark`主题菜单字体颜色
  '@menu-dark-item-active-bg': '#060e23' // `dark`主题菜单激活时的背景色
};

module.exports = customTheme;

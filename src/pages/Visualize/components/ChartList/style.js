import styled from 'styled-components';

const ListWrapper = styled.div`
  .ant-list {
    width: 50%;
  }

  .anticon {
    margin-top: 3px;
  }

  span {
    margin-left: 10px;
  }
`;

export { ListWrapper };
